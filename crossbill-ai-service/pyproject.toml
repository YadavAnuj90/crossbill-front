[project]
name = "crossbill-ai"
version = "0.1.0"
description = "FastAPI service for generating professional PDF documents"
requires-python = ">=3.12"

[tool.black]
line-length = 120

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 120

[tool.pytest.ini_options]
testpaths = ["tests"]
